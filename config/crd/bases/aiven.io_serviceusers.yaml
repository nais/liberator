---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: serviceusers.aiven.io
spec:
  group: aiven.io
  names:
    kind: ServiceUser
    listKind: ServiceUserList
    plural: serviceusers
    singular: serviceuser
  scope: Namespaced
  versions:
  - deprecated: true
    deprecationWarning: Simplified ServiceUser type copied from aiven-operator, not
      to be installed in clusters!
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ServiceUser creates a service user for accessing Aiven services.
          The resource name becomes the username in Aiven.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              accessControl:
                description: |-
                  AccessControl Service type specific access control rules for user.
                  When this block is present, the operator manages the full access-control scope it contains.
                properties:
                  valkeyAclCategories:
                    description: Command category rules. Order matters.
                    items:
                      type: string
                    type: array
                  valkeyAclChannels:
                    description: Glob-style patterns defining which pub/sub channels
                      can be accessed.
                    items:
                      type: string
                    type: array
                  valkeyAclCommands:
                    description: Rules for individual commands. Order matters.
                    items:
                      type: string
                    type: array
                  valkeyAclKeys:
                    description: Key access rules.
                    items:
                      type: string
                    type: array
                type: object
              connInfoSecretTarget:
                description: 'Secret configuration: where the operator publishes the
                  user''s connection details'
                properties:
                  name:
                    description: Name of the secret resource to be created. By default,
                      it is equal to the resource name
                    type: string
                required:
                - name
                type: object
              project:
                description: Identifies the project this resource belongs to
                type: string
              serviceName:
                description: Specifies the name of the service that this resource
                  belongs to
                type: string
              username:
                description: |-
                  Username of the service user on Aiven. Defaults to the resource name.
                  Set this to manage users whose names are not valid Kubernetes object names (e.g. containing underscores or uppercase).
                  Immutable after creation: to change it, delete and recreate the resource.
                  At most one resource may reference a given username.
                type: string
            required:
            - project
            - serviceName
            type: object
          status:
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of a ServiceUser state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              type:
                description: Type of the user account
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
